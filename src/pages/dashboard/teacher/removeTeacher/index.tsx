import React from "react";

const RemoveTeacher = () => {
  return (
    <div>
      <h2 className="text-2xl font-bold">Remove Teacher</h2>
    </div>
  );
};

export default React.memo(RemoveTeacher);
